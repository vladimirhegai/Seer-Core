{
  "name": "seer",
  "version": "0.1.0",
  "description": "Local-first AI codebase explainer — onboarding from months to days",
  "private": true,
  "type": "commonjs",
  "bin": {
    "seer": "./dist/cli/index.js"
  },
  "scripts": {
    "dev": "tsx src/cli/index.ts",
    "build": "tsc",
    "test": "npm run test:smoke && npm run test:parallel && npm run test:trackcd && npm run test:git && npm run test:regressions && npm run test:query-parity && npm run test:filters && npm run test:mcp",
    "test:smoke": "tsx tests/smoke.ts",
    "test:mcp": "npm run build && tsx tests/mcp-smoke.ts && tsx tests/mcp-jit.ts && tsx tests/mcp-watcher.ts && tsx tests/mcp-trackcd.ts",
    "test:parallel": "tsx tests/parallel-read.ts",
    "test:trackcd": "tsx tests/trackcd.ts",
    "test:git": "tsx tests/git-features.ts",
    "test:regressions": "tsx tests/bug-regressions.ts",
    "test:query-parity": "tsx tests/query-parity.ts",
    "test:filters": "tsx tests/filters.ts",
    "scale-test": "tsx tests/scale-test.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "chokidar": "^5.0.0",
    "commander": "^12.0.0",
    "fast-glob": "^3.3.2",
    "ignore": "^5.3.1",
    "tree-sitter-wasms": "^0.1.13",
    "web-tree-sitter": "^0.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^5.0.5",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
