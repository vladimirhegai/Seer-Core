version: '3.8'

services:
  notifier:
    image: notifier:latest
    ports:
      - 8080:8080
  legacy-api:
    image: legacy:latest
    ports:
      - 9000:9000
  payment-service:
    image: payment:latest
    ports:
      - 7000:7000
